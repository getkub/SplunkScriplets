Vagrant.configure("2") do |config|
  # Specify the box
  config.vm.box = "rockylinux/9"
  
  # Optional: Basic provider settings to ensure compatibility
  config.vm.provider "virtualbox" do |vb|
    vb.memory = 2048
    vb.cpus = 2
  end
end
